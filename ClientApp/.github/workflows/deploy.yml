# Disabled workflow: deploy to GitHub Pages
# Renamed to deploy.yml.disabled to prevent automatic GitHub Pages deployment

name: Deploy to GitHub Pages (DISABLED)

on:
  # push:
  #   branches:
  #     - main
  #     - master

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install and Build
        run: |
          npm ci --legacy-peer-deps 
          npm run build

      - name: Deploy
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
